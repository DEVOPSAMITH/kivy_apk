name: Build Kivy APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3  # ✅ This is required before calling local action

      - name: Build Kivy APK
        uses: ./.github/actions/kivy-to-apk
        with:
          work-dir: '.'  # or subfolder path if needed
